# KNUE_MoA → knue.mate 통합 (1단계) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** KNUE_MoA의 공지 크롤러·키워드 알림·학사일정을 knue.mate로 이식하고, 홈 대시보드 + 5탭(홈·식단·버스·지도·더보기) 구조로 재편한다.

**Architecture:** MoA의 크롤러 로직은 거의 그대로 복사하되 Hive 캐시를 SharedPreferences JSON 캐시로 교체. 상태관리는 knue.mate 기존 패턴(ValueNotifier + SharedPreferences, `PreferencesService`)으로 통일. 백그라운드 키워드 알림은 기존 workmanager 파이프라인에 task를 추가.

**Tech Stack:** Flutter, SharedPreferences, workmanager, flutter_local_notifications v21, table_calendar, http+html(크롤링), cp949_codec/euc(인코딩)

**Source repos:**
- 대상: `/Users/kaciaoz/Desktop/KNUE Mate/knue.mate` (여기서 작업, git repo)
- 원본: `/Users/kaciaoz/Desktop/KNUE Mate/KNUE_MoA` (읽기 전용 참조)

## Global Constraints

- 신규 상태관리/저장 라이브러리 금지: riverpod, hive, lucide_icons, uuid, flutter_staggered_animations 미도입
- 추가 의존성은 정확히: `table_calendar: ^3.1.3`, `cp949_codec: ^1.0.2`, `euc: ^1.0.6+8`
- 버전 변경: `flutter_local_notifications: ^17.0.0` → `^21.0.0`
- `flutter analyze` 에서 error/warning 0 유지 (info는 기존 174개 수준 이하)
- 모든 신규 화면은 기존 디자인 언어를 따름: `Theme.of(context).cardColor` 카드, `themeColor.value` 포인트 색, 다크모드 대응
- UI 문자열은 한국어
- Android `applicationId`는 절대 변경하지 않음
- 커밋 메시지 끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

---

### Task 1: 웹 크래시 수정 커밋 (이미 작업 트리에 있음)

**Files:**
- Modify (이미 수정됨, 커밋만): `lib/bus_screen.dart`, `lib/meal_screen.dart`, `lib/campus_map_screen.dart`

**Interfaces:**
- Consumes: 없음
- Produces: 웹에서 전 탭 크래시 없이 동작 (이후 태스크의 웹 검증 기반)

- [ ] **Step 1: 수정 내용 확인**

Run: `cd "/Users/kaciaoz/Desktop/KNUE Mate/knue.mate" && git diff --stat`
Expected: bus_screen.dart, meal_screen.dart, campus_map_screen.dart 3개 파일 (kIsWeb 가드 10곳)

- [ ] **Step 2: analyze 통과 확인**

Run: `flutter analyze --no-pub 2>&1 | tail -3`
Expected: error/warning 없음 (info만)

- [ ] **Step 3: 커밋**

```bash
git add lib/bus_screen.dart lib/meal_screen.dart lib/campus_map_screen.dart
git commit -m "fix: 웹에서 dart:io Platform 호출로 인한 크래시 수정 (kIsWeb 가드)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: 의존성 추가 및 flutter_local_notifications v21 마이그레이션

**Files:**
- Modify: `pubspec.yaml`
- Verify: `android/app/build.gradle` (desugaring), `android/app/src/main/AndroidManifest.xml` (receiver)
- 참조: `../KNUE_MoA/android/app/build.gradle`, `../KNUE_MoA/android/app/src/main/AndroidManifest.xml` (v21이 동작하는 설정)

**Interfaces:**
- Produces: `table_calendar`, `cp949_codec`, `euc` import 가능, `flutter_local_notifications` v21 API

- [ ] **Step 1: pubspec.yaml 수정**

`flutter_local_notifications: ^17.0.0` 줄을 `flutter_local_notifications: ^21.0.0` 으로 바꾸고, dependencies 블록에 추가:

```yaml
  table_calendar: ^3.1.3
  cp949_codec: ^1.0.2
  euc: ^1.0.6+8
```

- [ ] **Step 2: pub get 실행**

Run: `flutter pub get`
Expected: 해석 성공. 실패 시(버전 충돌) 충돌 패키지의 MoA pubspec.lock 버전으로 핀 고정

- [ ] **Step 3: Android 설정 비교**

Run: `grep -n "desugar\|coreLibraryDesugaring\|multiDex" android/app/build.gradle ../KNUE_MoA/android/app/build.gradle`
Run: `grep -n "receiver\|ScheduledNotification" android/app/src/main/AndroidManifest.xml ../KNUE_MoA/android/app/src/main/AndroidManifest.xml`

MoA에 있고 knue.mate에 없는 desugaring/receiver 설정이 있으면 그대로 복사. 둘 다 동일하면 스킵.

- [ ] **Step 4: 컴파일 확인**

Run: `flutter analyze --no-pub 2>&1 | tail -3`
Expected: error 없음 (v21 API 변경으로 constants.dart의 NotificationService가 깨지면 v21 시그니처로 수정 — MoA `lib/services/notification_service.dart` 가 v21 기준 동작 예시)

- [ ] **Step 5: 커밋**

```bash
git add pubspec.yaml pubspec.lock android/
git commit -m "chore: 공지/캘린더 의존성 추가, flutter_local_notifications v21 업그레이드

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Notice 모델

**Files:**
- Create: `lib/notice_model.dart`
- Test: `test/notice_model_test.dart`

**Interfaces:**
- Produces: `class Notice { int id; String category, group, title, date, author, link; bool isNew, isRead; Map<String,dynamic> toJson(); factory Notice.fromJson(Map); }`
- Produces: `class CalendarEvent { DateTime startDate, endDate; String title; }`

- [ ] **Step 1: 실패하는 테스트 작성** — `test/notice_model_test.dart`

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_application_1/notice_model.dart';

void main() {
  test('Notice JSON 왕복 직렬화', () {
    final n = Notice(
      id: 12345,
      category: '학사공지',
      group: 'MAIN',
      title: '2026학년도 2학기 수강신청 안내',
      date: '2026-07-15',
      author: '교무처',
      link: 'https://www.knue.ac.kr/www/selectBbsNttView.do?nttNo=12345',
      isNew: true,
    );
    final restored = Notice.fromJson(n.toJson());
    expect(restored.id, 12345);
    expect(restored.category, '학사공지');
    expect(restored.group, 'MAIN');
    expect(restored.title, '2026학년도 2학기 수강신청 안내');
    expect(restored.isNew, true);
    expect(restored.isRead, false);
  });
}
```

- [ ] **Step 2: 실패 확인**

Run: `flutter test test/notice_model_test.dart`
Expected: FAIL — `notice_model.dart` 없음

- [ ] **Step 3: 구현** — `lib/notice_model.dart`

MoA `lib/models/notice_model.dart` 기반, Hive 어노테이션 제거 + JSON 추가:

```dart
/// KNUE 게시판 공지 한 건. (KNUE_MoA notice_model.dart 이식, Hive 제거)
class Notice {
  final int id;
  final String category; // 게시판 이름 (예: 학사공지)
  final String group; // 게시판 그룹 (MAIN/ANNEX/LIFE/DEPT)
  final String title;
  final String date; // yyyy-MM-dd
  final String author;
  final String link;
  final bool isNew;
  bool isRead;

  Notice({
    required this.id,
    required this.category,
    required this.group,
    required this.title,
    required this.date,
    required this.author,
    required this.link,
    this.isNew = false,
    this.isRead = false,
  });

  Map<String, dynamic> toJson() => {
        'id': id,
        'category': category,
        'group': group,
        'title': title,
        'date': date,
        'author': author,
        'link': link,
        'isNew': isNew,
        'isRead': isRead,
      };

  factory Notice.fromJson(Map<String, dynamic> json) => Notice(
        id: json['id'] as int,
        category: json['category'] as String,
        group: json['group'] as String,
        title: json['title'] as String,
        date: json['date'] as String,
        author: json['author'] as String? ?? '',
        link: json['link'] as String? ?? '',
        isNew: json['isNew'] as bool? ?? false,
        isRead: json['isRead'] as bool? ?? false,
      );
}

/// 학사일정 이벤트 (KNUE_MoA scraper_service.dart의 CalendarEvent 이식)
class CalendarEvent {
  final DateTime startDate;
  final DateTime endDate;
  final String title;

  CalendarEvent({
    required this.startDate,
    required this.endDate,
    required this.title,
  });

  Map<String, dynamic> toJson() => {
        'start': startDate.toIso8601String(),
        'end': endDate.toIso8601String(),
        'title': title,
      };

  factory CalendarEvent.fromJson(Map<String, dynamic> json) => CalendarEvent(
        startDate: DateTime.parse(json['start'] as String),
        endDate: DateTime.parse(json['end'] as String),
        title: json['title'] as String,
      );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `flutter test test/notice_model_test.dart`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lib/notice_model.dart test/notice_model_test.dart
git commit -m "feat: Notice/CalendarEvent 모델 추가 (MoA 이식, JSON 직렬화)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 공지 크롤러 서비스 (KnueScraper 이식)

**Files:**
- Create: `lib/notice_service.dart`
- Test: `test/notice_service_test.dart`
- 원본: `../KNUE_MoA/lib/services/scraper_service.dart` (1,000줄가량 — 전체 복사 후 변형)

**Interfaces:**
- Consumes: `Notice`, `CalendarEvent` (Task 3)
- Produces:
  - `class KnueScraper` — `boardGroups: Map<String, Map<String, String>>` (MAIN/ANNEX/LIFE/DEPT/GRAD — GRAD는 대학원 계열 3개 게시판, 5번째 그룹으로 확정 2026-07-16)
  - `Future<List<Notice>> fetchAllNotices({bool forceRefresh = false, Set<String>? onlyCategories})` — onlyCategories 지정 시 해당 게시판만 크롤링 (홈 대시보드용)
  - `Future<List<CalendarEvent>> fetchCalendarEvents(int year, int month)`
  - `static List<Notice> parseHtml(Map<String, dynamic> params)` — 기존 `_parseHtmlStatic`을 public으로 (테스트용)
  - `class NoticeCache` — `static Future<void> save(List<Notice>)`, `static Future<List<Notice>?> load()`, `static Future<DateTime?> lastUpdated()`

- [ ] **Step 1: 원본 복사**

```bash
cp "../KNUE_MoA/lib/services/scraper_service.dart" lib/notice_service.dart
```

- [ ] **Step 2: Hive 제거 변형**

`lib/notice_service.dart` 에서:
1. `import 'package:hive/hive.dart';` 삭제, `import 'package:shared_preferences/shared_preferences.dart';` 추가
2. import 경로 수정: `package:knue_moa/models/notice_model.dart` → `notice_model.dart` (CalendarEvent 클래스 정의는 파일 상단에 중복돼 있으면 삭제하고 notice_model.dart 것 사용)
3. `noticeBoxName` 상수와 Hive 박스 관련 코드 전부 삭제
4. `fetchAllNotices` / `_fetchAndUpdateCache` 를 아래로 교체 (캐시 즉시 반환 + 백그라운드 갱신 로직 유지):

```dart
  /// 공지 가져오기. 캐시가 있으면 즉시 반환하고 백그라운드 갱신.
  /// [onlyCategories] 지정 시 해당 게시판만 크롤링(홈 대시보드 경량 경로).
  Future<List<Notice>> fetchAllNotices({
    bool forceRefresh = false,
    Set<String>? onlyCategories,
  }) async {
    if (!forceRefresh) {
      final cached = await NoticeCache.load();
      if (cached != null && cached.isNotEmpty) {
        _fetchAndUpdateCache(onlyCategories: onlyCategories); // await 없이 갱신
        return cached;
      }
    }
    return _fetchAndUpdateCache(onlyCategories: onlyCategories);
  }

  Future<List<Notice>> _fetchAndUpdateCache({Set<String>? onlyCategories}) async {
    List<Notice> all = [];
    List<Future<List<Notice>>> futures = [];

    for (var groupEntry in boardGroups.entries) {
      String groupName = groupEntry.key;
      for (var entry in groupEntry.value.entries) {
        if (onlyCategories != null && !onlyCategories.contains(entry.key)) {
          continue;
        }
        futures.add(
          _fetchBoardWithRetry(groupName, entry.key, entry.value)
              .catchError((e) {
            debugPrint('Error fetching $groupName - ${entry.key}: $e');
            return <Notice>[];
          }),
        );
      }
    }

    for (int i = 0; i < futures.length; i += 5) {
      int end = (i + 5 < futures.length) ? i + 5 : futures.length;
      final results = await Future.wait(futures.sublist(i, end));
      for (var res in results) {
        all.addAll(res);
      }
    }

    all.sort((a, b) => b.date.compareTo(a.date));

    if (onlyCategories == null) {
      await NoticeCache.save(all);
    } else if (all.isNotEmpty) {
      // 부분 크롤링은 기존 캐시에 병합
      final existing = await NoticeCache.load() ?? [];
      final ids = all.map((n) => n.id).toSet();
      existing.removeWhere((n) => ids.contains(n.id));
      final merged = [...all, ...existing]
        ..sort((a, b) => b.date.compareTo(a.date));
      await NoticeCache.save(merged);
    }
    return all;
  }
```

5. `_parseHtmlStatic` 이름을 `parseHtml` 로 변경 (public, `compute(parseHtml, ...)` 호출부도 함께)
6. 파일 끝에 NoticeCache 추가 (`OfflineCache` 패턴 준수):

```dart
/// 공지 캐시 — bus의 OfflineCache와 동일 패턴 (SharedPreferences + JSON)
class NoticeCache {
  static const _key = 'noticeCache';
  static const _maxItems = 500;

  static Future<void> save(List<Notice> notices) async {
    final prefs = await SharedPreferences.getInstance();
    final trimmed = notices.take(_maxItems).toList();
    await prefs.setString(
        _key, jsonEncode(trimmed.map((e) => e.toJson()).toList()));
    await prefs.setInt('${_key}_ts', DateTime.now().millisecondsSinceEpoch);
  }

  static Future<List<Notice>?> load() async {
    final prefs = await SharedPreferences.getInstance();
    final raw = prefs.getString(_key);
    if (raw == null) return null;
    try {
      final List<dynamic> list = jsonDecode(raw);
      return list
          .map((e) => Notice.fromJson(e as Map<String, dynamic>))
          .toList();
    } catch (_) {
      return null;
    }
  }

  static Future<DateTime?> lastUpdated() async {
    final prefs = await SharedPreferences.getInstance();
    final ts = prefs.getInt('${_key}_ts');
    return ts == null ? null : DateTime.fromMillisecondsSinceEpoch(ts);
  }
}
```

- [ ] **Step 3: 파서 테스트 작성** — `test/notice_service_test.dart`

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_application_1/notice_service.dart';

void main() {
  test('KNUE 표준 게시판 HTML 파싱', () {
    const html = '''
<table class="bbs_list"><tbody>
<tr>
  <td class="ta_c">1234</td>
  <td class="ta_l"><a href="selectBbsNttView.do?nttNo=98765">2026학년도 장학금 신청 안내</a><img src="new.gif" alt="새글"></td>
  <td>교무처</td>
  <td class="ta_c">2026-07-15</td>
</tr>
</tbody></table>''';
    final notices = KnueScraper.parseHtml({
      'html': html,
      'group': 'MAIN',
      'category': '학사공지',
      'baseUrl': 'https://www.knue.ac.kr/www/selectBbsNttList.do?bbsNo=26&key=807',
    });
    expect(notices, isNotEmpty);
    expect(notices.first.title, contains('장학금'));
    expect(notices.first.category, '학사공지');
  });

  test('boardGroups 4개 그룹 유지', () {
    final scraper = KnueScraper();
    expect(scraper.boardGroups.keys,
        containsAll(['MAIN', 'ANNEX', 'LIFE', 'DEPT']));
  });
}
```

주의: `parseHtml`의 실제 파라미터 키는 원본 `_parseHtmlStatic` 시그니처를 열어 확인하고 테스트를 맞출 것 (원본 파일 329행 부근). 파싱 셀렉터가 위 fixture와 다르면 fixture를 원본 파서에 맞게 수정 — 테스트의 목적은 회귀 방지지 파서 재설계가 아님.

- [ ] **Step 4: 테스트 실행**

Run: `flutter test test/notice_service_test.dart`
Expected: PASS (파서 시그니처에 맞게 조정 후)

- [ ] **Step 5: analyze + 커밋**

Run: `flutter analyze --no-pub 2>&1 | tail -3` → error/warning 0

```bash
git add lib/notice_service.dart test/notice_service_test.dart
git commit -m "feat: KNUE 공지 크롤러 이식 (Hive→SharedPreferences 캐시)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: AppTab 재편 + 공지 설정 저장소

**Files:**
- Modify: `lib/constants.dart` (AppTab enum 72-83행, PreferencesService 504행~)
- Test: `test/tab_migration_test.dart`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `enum AppTab { home("홈", Icons.home_rounded, Colors.indigo), meal(...), bus(...), run(...), map(...), more("더보기", Icons.apps_rounded, Colors.blueGrey), settings(...) }` — run/settings는 enum에 남기되 하단 탭 기본 순서에서 제외
  - `PreferencesService.tabOrder` 기본값 `[home, meal, bus, map, more]`
  - `static List<AppTab> migrateTabOrder(List<String> savedNames)` — 순수 함수
  - `static final ValueNotifier<List<String>> noticeKeywords` (기본 `['장학', '수강', '졸업']`)
  - `static final ValueNotifier<List<String>> favoriteBoards` (기본 `['대학소식', '학사공지']`)
  - `static final ValueNotifier<bool> noticeAlarmOn` (기본 true)
  - `saveNoticeKeywords(List<String>)`, `saveFavoriteBoards(List<String>)`, `saveNoticeAlarm(bool)` — 저장 키: `notice_keywords`, `fav_boards`, `notice_alarm_on`

- [ ] **Step 1: 마이그레이션 테스트 작성** — `test/tab_migration_test.dart`

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_application_1/constants.dart';

void main() {
  test('기존 5탭 저장값 → 새 5탭 구조 마이그레이션', () {
    final result = PreferencesService.migrateTabOrder(
        ['meal', 'bus', 'run', 'map', 'settings']);
    expect(result.first, AppTab.home); // 홈이 맨 앞에 삽입
    expect(result.last, AppTab.more); // 더보기가 맨 뒤에
    expect(result, isNot(contains(AppTab.run))); // run/settings는 더보기로 흡수
    expect(result, isNot(contains(AppTab.settings)));
    // 기존 사용자의 식단→버스→지도 상대 순서 유지
    expect(result.indexOf(AppTab.meal), lessThan(result.indexOf(AppTab.bus)));
    expect(result.indexOf(AppTab.bus), lessThan(result.indexOf(AppTab.map)));
  });

  test('알 수 없는 탭 이름은 무시', () {
    final result = PreferencesService.migrateTabOrder(['meal', 'ghost']);
    expect(result, contains(AppTab.home));
    expect(result, contains(AppTab.meal));
    expect(result, contains(AppTab.more));
  });

  test('신규 사용자(빈 저장값)는 기본 순서', () {
    final result = PreferencesService.migrateTabOrder([]);
    expect(result,
        [AppTab.home, AppTab.meal, AppTab.bus, AppTab.map, AppTab.more]);
  });
}
```

- [ ] **Step 2: 실패 확인**

Run: `flutter test test/tab_migration_test.dart`
Expected: FAIL — `migrateTabOrder` 미정의

- [ ] **Step 3: constants.dart 수정**

AppTab enum (72행)을 다음으로 교체:

```dart
enum AppTab {
  home("홈", Icons.home_rounded, Colors.indigo),
  meal("식단", Icons.restaurant_menu_rounded, Colors.orange),
  bus("버스", Icons.directions_bus_rounded, Colors.blue),
  run("런", Icons.directions_run_rounded, Colors.green),
  map("지도", Icons.map_rounded, Colors.deepPurple),
  more("더보기", Icons.apps_rounded, Colors.blueGrey),
  settings("설정", Icons.settings_rounded, Colors.grey);

  final String label;
  final IconData icon;
  final Color color;
  const AppTab(this.label, this.icon, this.color);
}
```

PreferencesService 내부: tabOrder 기본값 교체 + 마이그레이션 + 공지 설정 추가:

```dart
  static const String keyTabOrder = 'tab_order_v2'; // v1 → v2
  static const String keyNoticeKeywords = 'notice_keywords';
  static const String keyFavBoards = 'fav_boards';
  static const String keyNoticeAlarm = 'notice_alarm_on';

  static final ValueNotifier<List<AppTab>> tabOrder = ValueNotifier([
    AppTab.home,
    AppTab.meal,
    AppTab.bus,
    AppTab.map,
    AppTab.more,
  ]);

  static final ValueNotifier<List<String>> noticeKeywords =
      ValueNotifier(['장학', '수강', '졸업']);
  static final ValueNotifier<List<String>> favoriteBoards =
      ValueNotifier(['대학소식', '학사공지']);
  static final ValueNotifier<bool> noticeAlarmOn = ValueNotifier(true);

  /// 하단 탭에 배치 가능한 탭 (run/settings는 더보기 내부로)
  static const List<AppTab> navigableTabs = [
    AppTab.home,
    AppTab.meal,
    AppTab.bus,
    AppTab.map,
    AppTab.more,
  ];

  /// 저장된 탭 이름 목록 → 새 5탭 구조. 순수 함수(테스트 대상).
  static List<AppTab> migrateTabOrder(List<String> savedNames) {
    final mapped = savedNames
        .map((name) {
          try {
            return AppTab.values.firstWhere((t) => t.name == name);
          } catch (_) {
            return null;
          }
        })
        .whereType<AppTab>()
        .where((t) => navigableTabs.contains(t))
        .toList();
    final result = <AppTab>[];
    for (final t in mapped) {
      if (!result.contains(t)) result.add(t);
    }
    if (!result.contains(AppTab.home)) result.insert(0, AppTab.home);
    for (final t in navigableTabs) {
      if (!result.contains(t)) result.add(t);
    }
    // more는 항상 마지막
    result.remove(AppTab.more);
    result.add(AppTab.more);
    return result;
  }
```

`loadSettings()` 내 tabOrder 로드 부분(538행 부근)을 교체 — v1 키도 읽어 마이그레이션:

```dart
    final savedV2 = prefs.getStringList(keyTabOrder);
    final savedV1 = prefs.getStringList('tab_order_v1');
    if (savedV2 != null) {
      tabOrder.value = migrateTabOrder(savedV2);
    } else if (savedV1 != null) {
      tabOrder.value = migrateTabOrder(savedV1);
      await prefs.setStringList(
          keyTabOrder, tabOrder.value.map((t) => t.name).toList());
    }
    noticeKeywords.value =
        prefs.getStringList(keyNoticeKeywords) ?? ['장학', '수강', '졸업'];
    favoriteBoards.value =
        prefs.getStringList(keyFavBoards) ?? ['대학소식', '학사공지'];
    noticeAlarmOn.value = prefs.getBool(keyNoticeAlarm) ?? true;
```

save 메서드 추가 (기존 saveTabOrder 아래):

```dart
  static Future<void> saveNoticeKeywords(List<String> keywords) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setStringList(keyNoticeKeywords, keywords);
    noticeKeywords.value = List.from(keywords);
  }

  static Future<void> saveFavoriteBoards(List<String> boards) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setStringList(keyFavBoards, boards);
    favoriteBoards.value = List.from(boards);
  }

  static Future<void> saveNoticeAlarm(bool on) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setBool(keyNoticeAlarm, on);
    noticeAlarmOn.value = on;
  }
```

주의: 이 시점에 `root_screen.dart` 의 `_getScreenForTab` 스위치가 home/more 케이스 누락으로 컴파일 에러가 난다. 임시로 두 케이스에 `return const SizedBox();` 를 넣어 컴파일을 살리고, Task 9에서 실제 화면으로 교체.

- [ ] **Step 4: 테스트 통과 확인**

Run: `flutter test test/tab_migration_test.dart && flutter analyze --no-pub 2>&1 | tail -3`
Expected: PASS, error 0

- [ ] **Step 5: 커밋**

```bash
git add lib/constants.dart lib/root_screen.dart test/tab_migration_test.dart
git commit -m "feat: 5탭 구조(홈·식단·버스·지도·더보기) + 탭 마이그레이션 + 공지 설정 저장소

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: D-day / 개인 일정 저장소

**Files:**
- Create: `lib/schedule_model.dart`
- Modify: `lib/constants.dart` (PreferencesService에 저장 메서드)
- Test: `test/schedule_model_test.dart`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `class DdayItem { String id; String title; DateTime date; int daysLeft(DateTime now); toJson/fromJson }`
  - `class PersonalEvent { String id; String title; DateTime date; toJson/fromJson }`
  - `PreferencesService.ddayItems: ValueNotifier<List<DdayItem>>`, `personalEvents: ValueNotifier<List<PersonalEvent>>`
  - `saveDdayItems(List<DdayItem>)`, `savePersonalEvents(List<PersonalEvent>)` — 키: `dday_items`, `personal_events`

- [ ] **Step 1: 테스트 작성** — `test/schedule_model_test.dart`

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_application_1/schedule_model.dart';

void main() {
  test('DdayItem daysLeft 계산', () {
    final d = DdayItem(
        id: '1', title: '기말고사', date: DateTime(2026, 7, 20));
    expect(d.daysLeft(DateTime(2026, 7, 16)), 4);
    expect(d.daysLeft(DateTime(2026, 7, 20)), 0);
    expect(d.daysLeft(DateTime(2026, 7, 21)), -1);
  });

  test('DdayItem/PersonalEvent JSON 왕복', () {
    final d = DdayItem(id: 'a', title: '개강', date: DateTime(2026, 9, 1));
    expect(DdayItem.fromJson(d.toJson()).title, '개강');
    final p = PersonalEvent(
        id: 'b', title: '과제 마감', date: DateTime(2026, 7, 18));
    expect(PersonalEvent.fromJson(p.toJson()).date.day, 18);
  });
}
```

- [ ] **Step 2: 실패 확인**

Run: `flutter test test/schedule_model_test.dart` → FAIL

- [ ] **Step 3: 구현** — `lib/schedule_model.dart`

```dart
/// D-day 항목. id는 생성 시각 밀리초 문자열 (uuid 미도입 방침).
class DdayItem {
  final String id;
  final String title;
  final DateTime date;

  DdayItem({required this.id, required this.title, required this.date});

  /// 남은 일수. 오늘이면 0, 지났으면 음수.
  int daysLeft(DateTime now) {
    final today = DateTime(now.year, now.month, now.day);
    final target = DateTime(date.year, date.month, date.day);
    return target.difference(today).inDays;
  }

  Map<String, dynamic> toJson() =>
      {'id': id, 'title': title, 'date': date.toIso8601String()};

  factory DdayItem.fromJson(Map<String, dynamic> json) => DdayItem(
        id: json['id'] as String,
        title: json['title'] as String,
        date: DateTime.parse(json['date'] as String),
      );
}

/// 개인 일정 (캘린더 표시용)
class PersonalEvent {
  final String id;
  final String title;
  final DateTime date;

  PersonalEvent({required this.id, required this.title, required this.date});

  Map<String, dynamic> toJson() =>
      {'id': id, 'title': title, 'date': date.toIso8601String()};

  factory PersonalEvent.fromJson(Map<String, dynamic> json) => PersonalEvent(
        id: json['id'] as String,
        title: json['title'] as String,
        date: DateTime.parse(json['date'] as String),
      );
}
```

constants.dart PreferencesService에 추가 (import 'schedule_model.dart' 필요, dart:convert는 이미 있음):

```dart
  static const String keyDdayItems = 'dday_items';
  static const String keyPersonalEvents = 'personal_events';

  static final ValueNotifier<List<DdayItem>> ddayItems = ValueNotifier([]);
  static final ValueNotifier<List<PersonalEvent>> personalEvents =
      ValueNotifier([]);

  static Future<void> saveDdayItems(List<DdayItem> items) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setString(
        keyDdayItems, jsonEncode(items.map((e) => e.toJson()).toList()));
    ddayItems.value = List.from(items);
  }

  static Future<void> savePersonalEvents(List<PersonalEvent> events) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setString(
        keyPersonalEvents, jsonEncode(events.map((e) => e.toJson()).toList()));
    personalEvents.value = List.from(events);
  }
```

`loadSettings()` 에 로드 추가:

```dart
    try {
      final ddayRaw = prefs.getString(keyDdayItems);
      if (ddayRaw != null) {
        ddayItems.value = (jsonDecode(ddayRaw) as List)
            .map((e) => DdayItem.fromJson(e as Map<String, dynamic>))
            .toList();
      }
      final peRaw = prefs.getString(keyPersonalEvents);
      if (peRaw != null) {
        personalEvents.value = (jsonDecode(peRaw) as List)
            .map((e) => PersonalEvent.fromJson(e as Map<String, dynamic>))
            .toList();
      }
    } catch (e) {
      debugPrint('schedule load error: $e');
    }
```

- [ ] **Step 4: 테스트 통과 + analyze**

Run: `flutter test test/schedule_model_test.dart && flutter analyze --no-pub 2>&1 | tail -3`
Expected: PASS, error 0

- [ ] **Step 5: 커밋**

```bash
git add lib/schedule_model.dart lib/constants.dart test/schedule_model_test.dart
git commit -m "feat: D-day/개인 일정 모델 및 저장소

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: 키워드 알림 백그라운드 서비스

**Files:**
- Create: `lib/keyword_alert_service.dart`
- Modify: `lib/main.dart` (callbackDispatcher 분기 + task 등록)
- Test: `test/keyword_alert_test.dart`
- 원본 참조: `../KNUE_MoA/lib/services/notification_service.dart` 19-130행

**Interfaces:**
- Consumes: `KnueScraper.fetchAllNotices(forceRefresh: true)`, `Notice`, `NotificationService()` (constants.dart의 기존 싱글턴, `showNotification(int id, String title, String body)`)
- Produces:
  - `class KeywordAlertService`
  - `static List<Notice> filterNewMatches({required List<Notice> notices, required List<String> keywords, required List<String> favBoards, required Set<String> notifiedIds})` — 순수 함수
  - `static Future<void> checkAndNotify()` — 백그라운드 task 본체 (디버그 버튼에서도 호출)
  - `static Future<void> syncRegistration()` — noticeAlarmOn && 키워드 존재 시 periodic task 등록, 아니면 취소. task 이름 상수: `kNoticeCheckTask = 'knue_notice_check_task'`

- [ ] **Step 1: 매칭 로직 테스트** — `test/keyword_alert_test.dart`

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_application_1/keyword_alert_service.dart';
import 'package:flutter_application_1/notice_model.dart';

Notice _n(int id, String category, String title) => Notice(
    id: id, category: category, group: 'MAIN', title: title,
    date: '2026-07-16', author: '', link: '');

void main() {
  test('키워드 매칭 + 이미 알린 공지 제외', () {
    final result = KeywordAlertService.filterNewMatches(
      notices: [
        _n(1, '학사공지', '2학기 수강신청 안내'),
        _n(2, '학사공지', '도서관 휴관 안내'),
        _n(3, '장학금', '국가장학금 2차 신청'),
      ],
      keywords: ['수강', '장학'],
      favBoards: [],
      notifiedIds: {'3'},
    );
    expect(result.map((n) => n.id), [1]); // 2는 키워드 불일치, 3은 이미 알림
  });

  test('관심 게시판 지정 시 그 외 게시판 제외', () {
    final result = KeywordAlertService.filterNewMatches(
      notices: [_n(1, '학사공지', '수강 안내'), _n(2, '입찰공고', '수강 장비 입찰')],
      keywords: ['수강'],
      favBoards: ['학사공지'],
      notifiedIds: {},
    );
    expect(result.map((n) => n.id), [1]);
  });

  test('키워드 비어 있으면 관심 게시판 새 글 전부 매칭', () {
    final result = KeywordAlertService.filterNewMatches(
      notices: [_n(1, '학사공지', '아무 공지')],
      keywords: [],
      favBoards: ['학사공지'],
      notifiedIds: {},
    );
    expect(result, hasLength(1));
  });
}
```

- [ ] **Step 2: 실패 확인**

Run: `flutter test test/keyword_alert_test.dart` → FAIL

- [ ] **Step 3: 구현** — `lib/keyword_alert_service.dart`

```dart
import 'package:flutter/foundation.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:workmanager/workmanager.dart';
import 'constants.dart';
import 'notice_model.dart';
import 'notice_service.dart';

const String kNoticeCheckTask = 'knue_notice_check_task';

/// 온디바이스 키워드 알림 (MoA notification_service.dart 로직 이식, Hive 제거)
class KeywordAlertService {
  /// 새로 알릴 공지 선별. 순수 함수 — 테스트 대상.
  /// 키워드가 비어 있으면 관심 게시판의 새 글 전부가 대상.
  static List<Notice> filterNewMatches({
    required List<Notice> notices,
    required List<String> keywords,
    required List<String> favBoards,
    required Set<String> notifiedIds,
  }) {
    final result = <Notice>[];
    for (final notice in notices.take(50)) {
      if (notifiedIds.contains(notice.id.toString())) continue;
      final inFav = favBoards.isEmpty || favBoards.contains(notice.category);
      if (!inFav) continue;
      final matches = keywords.isEmpty ||
          keywords.any(
              (kw) => notice.title.toLowerCase().contains(kw.toLowerCase()));
      if (matches) result.add(notice);
    }
    return result;
  }

  /// 백그라운드 task 본체. 디버그 버튼에서도 직접 호출 가능.
  static Future<void> checkAndNotify() async {
    final prefs = await SharedPreferences.getInstance();
    if (!(prefs.getBool(PreferencesService.keyNoticeAlarm) ?? true)) return;

    final notices =
        await KnueScraper().fetchAllNotices(forceRefresh: true);
    if (notices.isEmpty) return;

    final keywords =
        prefs.getStringList(PreferencesService.keyNoticeKeywords) ?? [];
    final favBoards =
        prefs.getStringList(PreferencesService.keyFavBoards) ?? [];
    final notified = prefs.getStringList('notified_ids') ?? [];

    final newItems = filterNewMatches(
      notices: notices,
      keywords: keywords,
      favBoards: favBoards,
      notifiedIds: notified.toSet(),
    );

    // 스캔한 상위 50개는 전부 알림 완료로 기록 (반복 알림 방지)
    final allScanned = notices.take(50).map((n) => n.id.toString());
    final merged = {...notified, ...allScanned}.toList();
    final trimmed = merged.length > 200
        ? merged.sublist(merged.length - 200)
        : merged;
    await prefs.setStringList('notified_ids', trimmed);

    if (newItems.isEmpty) return;

    final ns = NotificationService();
    await ns.init();
    if (newItems.length == 1) {
      await ns.showNotification(
        newItems[0].id,
        '[${newItems[0].category}] 새 공지사항',
        newItems[0].title,
      );
    } else {
      await ns.showNotification(
        newItems[0].id,
        '새 공지사항 ${newItems.length}건',
        '[${newItems[0].category}] ${newItems[0].title} 외 ${newItems.length - 1}건',
      );
    }
  }

  /// 설정 상태에 맞춰 periodic task 등록/해제
  static Future<void> syncRegistration() async {
    if (kIsWeb) return;
    try {
      final on = PreferencesService.noticeAlarmOn.value &&
          PreferencesService.noticeKeywords.value.isNotEmpty;
      if (on) {
        await Workmanager().registerPeriodicTask(
          kNoticeCheckTask,
          kNoticeCheckTask,
          frequency: const Duration(hours: 2),
          constraints: Constraints(networkType: NetworkType.connected),
          existingWorkPolicy: ExistingPeriodicWorkPolicy.keep,
        );
      } else {
        await Workmanager().cancelByUniqueName(kNoticeCheckTask);
      }
    } catch (e) {
      debugPrint('keyword alert registration error: $e');
    }
  }
}
```

주의: `NotificationService.showNotification` 시그니처는 constants.dart의 기존 것(positional: id, title, body — main.dart 218행 사용 예 참조)을 그대로 사용. 존재하지 않으면 constants.dart의 NotificationService에 그 시그니처로 추가.

- [ ] **Step 4: main.dart 연결**

`callbackDispatcher` (26행)를 task 이름 분기로 교체:

```dart
@pragma('vm:entry-point')
void callbackDispatcher() {
  Workmanager().executeTask((task, inputData) async {
    try {
      await Firebase.initializeApp(
        options: DefaultFirebaseOptions.currentPlatform,
      );
      await PreferencesService.loadSettings();
      if (task == kNoticeCheckTask) {
        await KeywordAlertService.checkAndNotify();
      } else {
        final targetDate = getWidgetTargetDate(defaultSourceNotifier.value);
        await fetchMealApi(targetDate, defaultSourceNotifier.value);
      }
    } catch (e) {
      debugPrint("Workmanager error: $e");
    }
    return Future.value(true);
  });
}
```

main()의 Workmanager 등록 블록(124-136행) 안, 기존 registerPeriodicTask 다음에 추가:

```dart
        await KeywordAlertService.syncRegistration();
```

import 추가: `import 'keyword_alert_service.dart';`

- [ ] **Step 5: 테스트 + analyze + 커밋**

Run: `flutter test && flutter analyze --no-pub 2>&1 | tail -3`
Expected: 전체 PASS, error 0

```bash
git add lib/keyword_alert_service.dart lib/main.dart test/keyword_alert_test.dart
git commit -m "feat: 온디바이스 키워드 알림 (workmanager + 로컬 알림)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 8: 공지 화면

**Files:**
- Create: `lib/notice_screen.dart`
- Test: `test/notice_screen_test.dart`
- 참조: `../KNUE_MoA/lib/screens/home_page.dart` (HomeTab의 리스트 UI), `../KNUE_MoA/lib/widgets/notice_card.dart`, `keyword_chip.dart`

**Interfaces:**
- Consumes: `KnueScraper`, `Notice`, `NoticeCache.lastUpdated()`, `PreferencesService.noticeKeywords/favoriteBoards/saveNoticeKeywords/saveFavoriteBoards`, `KeywordAlertService.syncRegistration()`
- Produces: `class NoticeScreen extends StatefulWidget` — 홈 카드의 "전체 보기"에서 `Navigator.push`로 진입

**화면 구성:**
- AppBar: 제목 "청람공지", actions: 키워드 관리(bottom sheet), 새로고침
- 상단: 게시판 그룹 가로 칩 (전체/대학소식/학사공지/… — `scraper.boardGroups` 순회, 즐겨찾기 게시판 먼저, 별 아이콘으로 즐겨찾기 토글)
- 본문: 선택 게시판(또는 전체)의 공지 리스트. 카드: 제목(굵게, 2줄), 카테고리 칩(themeColor), 날짜, NEW 뱃지(isNew). 탭 → `url_launcher`로 원문
- RefreshIndicator로 당겨서 새로고침 (`forceRefresh: true`)
- 하단: 마지막 갱신 시각 (`NoticeCache.lastUpdated()`)
- 실패 게시판: 리스트 상단에 "일부 게시판을 불러오지 못했습니다" 배너 (fetchAllNotices의 catchError가 빈 리스트를 반환하므로, 카테고리별 결과 없음+선택 상태로 감지)
- 키워드 관리 bottom sheet: 현재 키워드 chip 목록(x로 삭제), TextField+추가 버튼, 변경 시 `saveNoticeKeywords` + `KeywordAlertService.syncRegistration()`

구현 스타일: `StatefulWidget` + `setState`, `ValueListenableBuilder<Color>(themeColor)` 래핑 — bus_screen.dart 575행 build 패턴을 그대로 따를 것. 코드 분량이 크므로 이 태스크는 파일 골격(위 구성 요소를 각각 `_buildBoardChips`, `_buildNoticeList`, `_buildNoticeCard`, `_showKeywordSheet` 메서드로)을 만들고 순서대로 채운다.

- [ ] **Step 1: 스모크 테스트 작성** — `test/notice_screen_test.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:flutter_application_1/notice_screen.dart';

void main() {
  testWidgets('NoticeScreen 렌더링 스모크', (tester) async {
    SharedPreferences.setMockInitialValues({});
    await tester.pumpWidget(const MaterialApp(home: NoticeScreen()));
    await tester.pump(const Duration(milliseconds: 100));
    expect(find.text('청람공지'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 실패 확인** → `flutter test test/notice_screen_test.dart` FAIL

- [ ] **Step 3: notice_screen.dart 구현**

위 화면 구성대로. 핵심 골격:

```dart
import 'package:flutter/material.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:url_launcher/url_launcher.dart';
import 'constants.dart';
import 'keyword_alert_service.dart';
import 'notice_model.dart';
import 'notice_service.dart';

class NoticeScreen extends StatefulWidget {
  const NoticeScreen({super.key});
  @override
  State<NoticeScreen> createState() => _NoticeScreenState();
}

class _NoticeScreenState extends State<NoticeScreen> {
  final _scraper = KnueScraper();
  List<Notice> _notices = [];
  bool _loading = true;
  String? _selectedCategory; // null = 전체
  DateTime? _lastUpdated;

  @override
  void initState() {
    super.initState();
    _load();
  }

  Future<void> _load({bool force = false}) async {
    setState(() => _loading = true);
    try {
      final list = await _scraper.fetchAllNotices(forceRefresh: force);
      final ts = await NoticeCache.lastUpdated();
      if (mounted) {
        setState(() {
          _notices = list;
          _lastUpdated = ts;
          _loading = false;
        });
      }
    } catch (e) {
      if (mounted) setState(() => _loading = false);
    }
  }
  // _buildBoardChips / _buildNoticeList / _buildNoticeCard / _showKeywordSheet
  // 는 위 "화면 구성" 명세대로 구현. AppBar 스타일은 bus_screen.dart 580-598행 패턴.
}
```

- [ ] **Step 4: 테스트 통과 + analyze** → PASS, error 0

- [ ] **Step 5: 커밋**

```bash
git add lib/notice_screen.dart test/notice_screen_test.dart
git commit -m "feat: 공지 화면 (게시판 그룹/즐겨찾기/키워드 관리)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 9: 캘린더 화면

**Files:**
- Create: `lib/calendar_screen.dart`
- Test: `test/calendar_screen_test.dart`

**Interfaces:**
- Consumes: `KnueScraper.fetchCalendarEvents(year, month)`, `CalendarEvent`, `DdayItem`, `PersonalEvent`, `PreferencesService.ddayItems/personalEvents/saveDdayItems/savePersonalEvents`
- Produces: `class CalendarScreen extends StatefulWidget`

**화면 구성:**
- AppBar "청람일정", actions: D-day 추가 버튼
- 상단: `TableCalendar` (월 뷰, 한국어 locale) — eventLoader가 학사일정(월별 fetch, `Map<int, List<CalendarEvent>>` 캐시) + 개인일정 병합
- 선택 날짜의 이벤트 리스트 (학사일정은 테마색 칩, 개인일정은 회색 칩 + 길게 눌러 삭제)
- 개인일정 추가: 날짜 선택 후 FAB → 제목 입력 다이얼로그, id는 `DateTime.now().millisecondsSinceEpoch.toString()`
- 하단 섹션: D-day 카드 리스트 (`daysLeft` 기준 정렬, "D-4"/"D-DAY"/"D+2" 표기, 스와이프 삭제)
- D-day 추가 다이얼로그: 제목 + showDatePicker

- [ ] **Step 1: 스모크 테스트** — `test/calendar_screen_test.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:intl/date_symbol_data_local.dart';
import 'package:flutter_application_1/calendar_screen.dart';

void main() {
  testWidgets('CalendarScreen 렌더링 스모크', (tester) async {
    SharedPreferences.setMockInitialValues({});
    await initializeDateFormatting('ko_KR');
    await tester.pumpWidget(const MaterialApp(home: CalendarScreen()));
    await tester.pump(const Duration(milliseconds: 100));
    expect(find.text('청람일정'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 실패 확인** → FAIL
- [ ] **Step 3: 구현** — 위 화면 구성대로, `table_calendar` 사용, 상태는 setState
- [ ] **Step 4: 테스트 통과 + analyze** → PASS, error 0
- [ ] **Step 5: 커밋**

```bash
git add lib/calendar_screen.dart test/calendar_screen_test.dart
git commit -m "feat: 학사일정 캘린더 + 개인일정 + D-day 화면

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 10: 홈 대시보드

**Files:**
- Create: `lib/home_screen.dart`
- Test: `test/home_screen_test.dart`

**Interfaces:**
- Consumes:
  - `fetchMealApi(DateTime, MealSource)` → `{'meals': {'breakfast': List, 'lunch': List, 'dinner': List}}` (constants.dart)
  - `getWidgetTargetDate(MealSource)`, `defaultSourceNotifier`, `MealType`, `statusFor()` (constants.dart)
  - `BusTimetableData.getNextBusTime(String route, bool isOutgoing, bool isWeekday)` → "HH:mm"?
  - `KnueScraper.fetchAllNotices(onlyCategories: PreferencesService.favoriteBoards.value.toSet())`
  - `KnueScraper.fetchCalendarEvents(year, month)`, `PreferencesService.ddayItems`
  - `RootNavigationScreen.switchTab(AppTab)` (root_screen.dart)
  - `NoticeScreen`, `CalendarScreen` (Navigator.push)
- Produces: `class HomeScreen extends StatefulWidget`

**화면 구성 (세로 스크롤, 각 섹션 독립 위젯):**
1. `_TodayBriefingHeader` — 날짜(intl `M월 d일 EEEE`, ko_KR), 인사말. 그 아래 가로 2카드:
   - 식단 카드: 현재 시각 기준 다음 끼니(`statusFor` 활용: 아침 전→아침, 점심 전→점심, 저녁 전→저녁, 저녁 후→내일 아침) 메뉴 상위 3줄. 탭 → `RootNavigationScreen.switchTab(AppTab.meal)`
   - 버스 카드: 513/514/518 중 가장 빠른 다음 출발 "513번 · 07:15" (`getNextBusTime`, isOutgoing: true, 요일은 `DateTime.now().weekday <= 5`). 전부 null이면 "운행 종료". 탭 → bus 탭
2. `_KeywordAlertsCard` — 캐시 공지 중 키워드 매칭 최근 3건. 키워드 없으면 "관심 키워드를 등록해 보세요" + 등록 버튼(NoticeScreen의 키워드 시트로)
3. `_NoticePreviewCard` — 즐겨찾는 게시판 최근 5건 (제목·카테고리·날짜), "전체 보기" → `Navigator.push(NoticeScreen())`
4. `_UpcomingCard` — 이번 달 학사일정 중 오늘 이후 3건 + D-day 상위 2건, "전체 보기" → `Navigator.push(CalendarScreen())`
5. 하단 여백 (2·3단계 카드 자리 — 코드 주석 `// 2단계: 동아리 공연 카드 / 3단계: 자취방 카드 삽입 지점`)

각 카드는 `Card` + 로딩 시 `CircularProgressIndicator`, 실패 시 "불러오기 실패 · 다시 시도" 텍스트버튼. 데이터 로드는 initState에서 병렬 시작, 카드별 독립 FutureBuilder (한 카드 실패가 다른 카드를 막지 않게).

- [ ] **Step 1: 스모크 테스트** — `test/home_screen_test.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:intl/date_symbol_data_local.dart';
import 'package:flutter_application_1/home_screen.dart';

void main() {
  testWidgets('HomeScreen 렌더링 스모크', (tester) async {
    SharedPreferences.setMockInitialValues({});
    await initializeDateFormatting('ko_KR');
    await tester.pumpWidget(const MaterialApp(home: HomeScreen()));
    await tester.pump(const Duration(milliseconds: 100));
    expect(find.byType(HomeScreen), findsOneWidget);
  });
}
```

- [ ] **Step 2: 실패 확인** → FAIL
- [ ] **Step 3: 구현** — 위 명세대로. 주의: fetchMealApi는 Firebase 접근을 포함하므로 테스트에선 네트워크 실패 → 실패 상태 렌더가 정상 (스모크 테스트는 이를 허용하는 구조)
- [ ] **Step 4: 테스트 통과 + analyze** → PASS, error 0
- [ ] **Step 5: 커밋**

```bash
git add lib/home_screen.dart test/home_screen_test.dart
git commit -m "feat: 홈 대시보드 (오늘 브리핑·키워드 알림·공지·일정 카드)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 11: 더보기 화면 + 루트 연결

**Files:**
- Create: `lib/more_screen.dart`, `lib/staff_contacts_screen.dart`
- Modify: `lib/root_screen.dart` (66-79행 `_getScreenForTab`), `lib/tab_edit_screen.dart` (필요 시)
- Test: `test/more_screen_test.dart`

**Interfaces:**
- Consumes: `CampusRunScreen`, `SettingsPage`(meal_screen.dart 1192행), `NoticeScreen`, `CalendarScreen`, `HomeScreen`, admin_staff_data.dart의 데이터 구조(파일 열어 실제 타입 확인 후 사용)
- Produces: `class MoreScreen`, `class StaffContactsScreen`

**MoreScreen 구성:** ListView, 섹션 헤더 "기능" — 캠퍼스런/교직원 연락처/청람공지/청람일정 4개 진입 타일 (아이콘+제목+chevron, Navigator.push), 섹션 헤더 "앱" — 설정 타일(SettingsPage push), 하단 앱 버전. 디버그 모드(kDebugMode)에서만: "키워드 알림 즉시 테스트" 타일 → `KeywordAlertService.checkAndNotify()` 호출 + 스낵바.

**StaffContactsScreen 구성:** admin_staff_data.dart 데이터를 부서별 ExpansionTile 그룹으로, 각 항목 이름·직위·전화(탭→`url_launcher` tel:). 상단 TextField 검색(이름/부서 contains 필터).

**root_screen.dart 수정:**

```dart
  Widget _getScreenForTab(AppTab tab) {
    switch (tab) {
      case AppTab.home:
        return const HomeScreen();
      case AppTab.meal:
        return const MealTabPage();
      case AppTab.bus:
        return const BusAppScreen();
      case AppTab.run:
        return const CampusRunScreen();
      case AppTab.map:
        return const CampusMapScreen();
      case AppTab.more:
        return const MoreScreen();
      case AppTab.settings:
        return const SettingsPage();
    }
  }
```

(run/settings 케이스는 enum이 남아 있으므로 유지 — 탭 순서에는 등장하지 않음)

tab_edit_screen.dart: `PreferencesService.tabOrder.value` 기반이므로 대체로 무수정. `AppTab.values` 를 직접 순회하는 코드가 있으면 `PreferencesService.navigableTabs` 로 교체.

- [ ] **Step 1: 스모크 테스트** — `test/more_screen_test.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_application_1/more_screen.dart';

void main() {
  testWidgets('MoreScreen 진입 타일 렌더링', (tester) async {
    await tester.pumpWidget(const MaterialApp(home: MoreScreen()));
    await tester.pump();
    expect(find.text('캠퍼스런'), findsOneWidget);
    expect(find.text('교직원 연락처'), findsOneWidget);
    expect(find.text('설정'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 실패 확인** → FAIL
- [ ] **Step 3: 구현** (more_screen.dart, staff_contacts_screen.dart, root_screen.dart 수정)
- [ ] **Step 4: 전체 테스트 + analyze** → `flutter test` 전체 PASS, error 0
- [ ] **Step 5: 커밋**

```bash
git add lib/more_screen.dart lib/staff_contacts_screen.dart lib/root_screen.dart lib/tab_edit_screen.dart test/more_screen_test.dart
git commit -m "feat: 더보기 탭(런·연락처·설정 통합) + 5탭 루트 연결

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 12: UX 개선 — 식단 이중 하단바 제거 + 탭바 대비

**Files:**
- Modify: `lib/meal_screen.dart` (MealTabPage 15-100행), `lib/root_screen.dart` (114-115행 색상)

**Interfaces:**
- Consumes/Produces: 기존 그대로 (시각 변경만)

- [ ] **Step 1: MealTabPage 하단 서브탭 → 상단 세그먼트**

MealTabPage의 `bottomNavigationBar:` 블록(57-72행)을 삭제하고, body를 Column으로 감싸 상단에 세그먼트 배치:

```dart
  @override
  Widget build(BuildContext context) {
    final color = themeColor.value;
    return Scaffold(
      body: Column(
        children: [
          SafeArea(
            bottom: false,
            child: Padding(
              padding: const EdgeInsets.fromLTRB(16, 8, 16, 0),
              child: SegmentedButton<int>(
                segments: const [
                  ButtonSegment(value: 0, label: Text('오늘'), icon: Icon(Icons.restaurant, size: 16)),
                  ButtonSegment(value: 1, label: Text('월간'), icon: Icon(Icons.calendar_month, size: 16)),
                ],
                selected: {_currentIndex},
                onSelectionChanged: (s) => _onTabTapped(s.first),
                style: SegmentedButton.styleFrom(selectedBackgroundColor: color.withOpacity(0.15)),
              ),
            ),
          ),
          Expanded(
            child: PageView(
              controller: _pageController,
              onPageChanged: (idx) => setState(() => _currentIndex = idx),
              children: _pages,
            ),
          ),
        ],
      ),
    );
  }
```

주의: TodayMealPage는 자체 SliverAppBar를 가지므로 세그먼트가 그 위에 얹히면 어색할 수 있음 — 실행해 보고 세그먼트를 SliverAppBar의 bottom 영역으로 옮기는 편이 나으면 그렇게 조정 (판단 기준: 오늘 페이지 스크롤 시 세그먼트 고정 여부).

- [ ] **Step 2: 하단 탭 대비 개선**

root_screen.dart 115행:

```dart
              unselectedItemColor: isDark ? Colors.white54 : Colors.black45,
```

- [ ] **Step 3: 웹으로 육안 확인**

Run: `flutter run -d web-server --web-port=8123` (백그라운드) 후 브라우저에서 식단 탭 확인
Expected: 하단바 1개만, 상단 세그먼트로 오늘/월간 전환 동작

- [ ] **Step 4: analyze + 커밋**

```bash
git add lib/meal_screen.dart lib/root_screen.dart
git commit -m "ux: 식단 이중 하단바 제거(상단 세그먼트화), 탭바 비선택 대비 개선

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 13: 패키지명 정리 + 최종 검증

**Files:**
- Modify: `pubspec.yaml` (name), `web/manifest.json`, `web/index.html` (title), 모든 `package:flutter_application_1/` import
- Test: 전체

- [ ] **Step 1: Dart 패키지명 변경**

pubspec.yaml `name: flutter_application_1` → `name: knue_mate`.

Run: `grep -rl "package:flutter_application_1" lib test`
각 파일에서 `package:flutter_application_1/` → `package:knue_mate/` 일괄 치환. web/index.html `<title>`과 web/manifest.json의 name/short_name을 "KNUE Mate"로.

- [ ] **Step 2: 전체 테스트**

Run: `flutter pub get && flutter test && flutter analyze --no-pub 2>&1 | tail -3`
Expected: 전체 PASS, error/warning 0

- [ ] **Step 3: 웹 종합 검증 (배포 전 필수)**

Run: `flutter run -d web-server --web-port=8123` 후 브라우저에서:
1. 첫 화면이 홈 대시보드 (5탭: 홈·식단·버스·지도·더보기)
2. 홈 → 공지 전체 보기 → 공지 리스트 로드 (실제 KNUE 서버 — 웹은 CORS로 실패할 수 있음, 그 경우 "불러오기 실패" 표시가 정상 동작인지 확인)
3. 캘린더 진입, D-day 추가/삭제
4. 더보기 → 캠퍼스런/연락처/설정 진입
5. 식단·버스·지도 기존 기능 회귀 없음

- [ ] **Step 4: 모바일 실검증 (가능하면)**

Run: `flutter devices` 로 연결된 Android 기기/에뮬레이터 확인 후 `flutter run -d <device>`
공지 로드(CORS 없음), 키워드 알림 즉시 테스트(더보기 디버그 타일) 확인

- [ ] **Step 5: 커밋**

```bash
git add -A
git commit -m "chore: 패키지명 knue_mate로 정리, 웹 타이틀 정비

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Self-Review 체크 결과

- **Spec coverage:** 공지 크롤러+피드(T4,T8), 키워드 알림(T7), 학사일정+D-day(T6,T9), 홈 대시보드(T10), 5탭 재편+마이그레이션(T5,T11), UX 개선(T12), 웹 크래시(T1), 의존성(T2), 패키지명(T13) — 스펙 전 항목 커버
- **알려진 유보:** 식단 빈 상태 메시지 원인 구분(스펙 "경미")은 T12에 포함하지 않음 — 후속 정리 항목으로 남김
- **Type consistency:** `Notice.id: int` (T3=T4=T7), `showNotification(int, String, String)` positional (T7, main.dart 기존 사용과 일치), `migrateTabOrder(List<String>) → List<AppTab>` (T5 테스트=구현), `getNextBusTime(String, bool, bool) → String?` (T10, 기존 코드 확인 완료)
